# iotsploit-django

`iotsploit-django` 是 IoTSploit 项目的 **Django 外圈（ring）**包：负责 HTTP/WS、ORM、Celery、Redis、以及对 `iotsploit-core` / `iosploit-fuzzer` 的组装注入（composition root）。

> 阶段 1（当前）：仅提供包骨架与最小可启动入口；业务代码仍由现有 `sat_toolkit` 承载，后续阶段再逐步迁移。


